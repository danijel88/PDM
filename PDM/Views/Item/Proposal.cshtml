@model IEnumerable<PDM.ViewModels.PropospalViewModel>

@{
    ViewData["Title"] = "proposals List";
}
<br />
<a asp-action="Index" class="btn btn-info"><i class="glyphicon glyphicon-arrow-left"></i> Back To The Items</a><br /><br />
<a id="createItem" data-toggle="modal" asp-action="" data-target="#myModal" class="btn btn-success">
    <i class="glyphicon glyphicon-plus"></i> Add New
</a>
<br /><br />
@if (Model.Count() == 0)
{
    <p>There is no proposals.</p>
}